name: Apply PCNA home card and nav update

on:
  push:
    branches: [main]
    paths:
      - 'scripts/apply_home_card_nav_update.py'
      - '.github/workflows/apply-home-card-nav-update.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Apply requested UI changes only
        run: python scripts/apply_home_card_nav_update.py
      - name: Compile app
        run: python -m py_compile app.py
      - name: Commit app update
        run: |
          if git diff --quiet; then
            echo "No patch required"
            exit 0
          fi
          git config user.name "PCNA App Automation"
          git config user.email "actions@users.noreply.github.com"
          git add app.py
          git commit -m "Update PCNA home cards and bottom navigation"
          git push origin main
